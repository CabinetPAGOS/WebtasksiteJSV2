<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site en Maintenance</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            margin: 0;
            background-color: #f2f2f2;
            color: #444;
            text-align: center;
        }
        h1 {
            font-size: 2.5em;
            color: #333;
            margin: 0;
        }
        p {
            font-size: 1.2em;
            margin: 10px 0 20px;
        }
        .loader {
            border: 8px solid #f3f3f3;
            border-top: 8px solid #3498db;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            animation: spin 1s linear infinite;
            margin-top: 20px;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        footer {
            position: absolute;
            bottom: 10px;
            font-size: 0.9em;
            color: #888;
        }
        #timer {
            font-size: 1.5em;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h1>Site en Maintenance</h1>
    <p>Nous revenons bientôt. Merci de votre patience.</p>
    <div class="loader"></div>
    <div id="timer">15:00</div>
    <footer>
        &copy; 2024 Pagos. Tous droits réservés.
    </footer>

    <script>
        let timeLeft = 15 * 60; // 15 minutes in seconds
        const timerDisplay = document.getElementById('timer');

        const countdown = setInterval(() => {
            const minutes = Math.floor(timeLeft / 60);
            const seconds = timeLeft % 60;
            timerDisplay.textContent = `${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`;

            if (timeLeft <= 0) {
                clearInterval(countdown);
                alert("Le temps est écoulé. Veuillez relancer l'URL du site pour vous reconnecter.");
                // Ici vous pouvez rediriger vers l'URL de votre site si nécessaire
                // window.location.href = "https://votre-url.com";
            }

            timeLeft--;
        }, 1000);
    </script>
</body>
</html>
